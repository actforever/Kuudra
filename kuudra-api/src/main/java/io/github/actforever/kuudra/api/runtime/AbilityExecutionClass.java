package io.github.actforever.kuudra.api.runtime;

/** DATA work follows the kernel pause gate; CONTROL work remains available while it is paused. */
public enum AbilityExecutionClass { DATA, CONTROL }
