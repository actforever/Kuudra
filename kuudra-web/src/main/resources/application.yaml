spring:
  application:
    name: kuudra-web
server:
  port: 8080

knife4j:
  enable: true
  setting:
    enable-group: true
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /doc.html
    urls-primary-name: all
