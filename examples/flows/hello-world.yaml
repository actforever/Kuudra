id: hello-world

components:
  loop-emitter:
    type: event-source
    component: hello-world/loop-emitter
    target: session
    enabled: true
  session:
    type: session-allocator
    options:
      name: hello-world-loop
      admissionKey: hello-world-loop
      policy: IGNORE
  print:
    type: actor
    component: hello-world/console-printer

routes:
  - from: session
    to: print
